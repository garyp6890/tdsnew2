<!doctype html>
<html lang="{{ shop.locale }}">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Site Under Construction | That Detail Shop</title>
    {{ content_for_header }}
    <style>
      * { box-sizing: border-box; }
      body {
        margin: 0;
        padding: 0;
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        background: #000000;
        color: #FFFFFF;
        min-height: 100vh;
        position: relative;
        overflow-x: hidden;
      }

      /* Video Background */
      .video-background {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: 1;
        overflow: hidden;
      }

      .video-background video {
        width: 100%;
        height: 100%;
        object-fit: cover;
      }

      /* Dark overlay for text readability */
      .video-background::after {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.6);
        z-index: 2;
      }

      .container {
        position: relative;
        z-index: 10;
        max-width: 600px;
        margin: 0 auto;
        padding: 2rem 1.5rem;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .lock-card {
        background: rgba(0, 0, 0, 0.85);
        border: 2px solid rgba(119, 54, 229, 0.3);
        border-radius: 16px;
        padding: 3rem 2.5rem;
        text-align: center;
        backdrop-filter: blur(15px);
        box-shadow: 0 25px 50px rgba(119, 54, 229, 0.2);
        width: 100%;
      }

      .logo {
        font-size: 2.8rem;
        font-weight: 900;
        margin-bottom: 1rem;
        background: linear-gradient(135deg, #7736e5, #3cff00);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        letter-spacing: -1px;
      }

      .headline {
        font-size: 2.2rem;
        font-weight: 800;
        margin-bottom: 1rem;
        line-height: 1.2;
        color: #FFFFFF;
      }

      .subtext {
        font-size: 1.1rem;
        color: #c9cace;
        margin-bottom: 2.5rem;
        line-height: 1.5;
      }

      .cta-primary {
        background: linear-gradient(135deg, #7736e5, #3cff00);
        color: #000000;
        padding: 1rem 2.5rem;
        border-radius: 8px;
        text-decoration: none;
        font-weight: 700;
        font-size: 1.1rem;
        display: inline-block;
        margin-bottom: 2rem;
        transition: all 0.3s ease;
        border: none;
        cursor: pointer;
        box-shadow: 0 4px 15px rgba(119, 54, 229, 0.4);
      }

      .cta-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(119, 54, 229, 0.6);
        background: linear-gradient(135deg, #3cff00, #7736e5);
      }

      .email-section {
        background: rgba(119, 54, 229, 0.1);
        border: 1px solid rgba(119, 54, 229, 0.3);
        border-radius: 12px;
        padding: 2rem;
        margin: 2rem 0;
      }

      .email-section h3 {
        color: #3cff00;
        font-size: 1.3rem;
        margin-bottom: 1rem;
        font-weight: 700;
      }

      .email-form {
        display: flex;
        gap: 0.5rem;
        margin-top: 1rem;
        flex-wrap: wrap;
        justify-content: center;
      }

      .email-input {
        flex: 1;
        min-width: 200px;
        padding: 0.8rem 1rem;
        border: 1px solid rgba(201, 202, 206, 0.3);
        border-radius: 6px;
        background: rgba(0, 0, 0, 0.3);
        color: #FFFFFF;
        font-size: 1rem;
      }

      .email-input::placeholder {
        color: #c9cace;
      }

      .email-submit {
        background: linear-gradient(135deg, #3cff00, #1b6005);
        color: #000000;
        border: none;
        padding: 0.8rem 1.5rem;
        border-radius: 6px;
        font-weight: 700;
        cursor: pointer;
        transition: all 0.3s ease;
      }

      .email-submit:hover {
        transform: translateY(-1px);
        box-shadow: 0 4px 15px rgba(60, 255, 0, 0.4);
      }

      .social-section {
        margin: 2.5rem 0;
      }

      .social-section h3 {
        color: #7736e5;
        font-size: 1.2rem;
        margin-bottom: 1.5rem;
        font-weight: 600;
      }

      .social-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
        gap: 1rem;
        margin-bottom: 2rem;
      }

      .social-link {
        background: rgba(0, 0, 0, 0.4);
        color: #FFFFFF;
        text-decoration: none;
        padding: 0.8rem 1rem;
        border-radius: 8px;
        border: 1px solid rgba(201, 202, 206, 0.2);
        transition: all 0.3s ease;
        font-weight: 500;
        font-size: 0.95rem;
      }

      .social-link:hover {
        background: rgba(119, 54, 229, 0.3);
        border-color: #7736e5;
        transform: translateY(-2px);
        box-shadow: 0 4px 15px rgba(119, 54, 229, 0.3);
      }

      .affiliate-section {
        background: rgba(60, 255, 0, 0.1);
        border: 1px solid rgba(60, 255, 0, 0.2);
        border-radius: 12px;
        padding: 1.5rem;
        margin: 2rem 0;
      }

      .affiliate-section h3 {
        color: #3cff00;
        margin-bottom: 1rem;
        font-weight: 600;
      }

      .affiliate-links {
        display: flex;
        gap: 1.5rem;
        justify-content: center;
        flex-wrap: wrap;
      }

      .affiliate-link {
        color: #FFFFFF;
        text-decoration: none;
        font-weight: 600;
        padding: 0.5rem 1rem;
        border-radius: 6px;
        transition: all 0.3s ease;
      }

      .affiliate-link.amazon {
        background: rgba(119, 54, 229, 0.3);
        border: 1px solid rgba(119, 54, 229, 0.5);
      }

      .affiliate-link.walmart {
        background: rgba(27, 96, 5, 0.5);
        border: 1px solid rgba(60, 255, 0, 0.3);
      }

      .affiliate-link:hover {
        transform: translateY(-2px);
        box-shadow: 0 4px 15px rgba(60, 255, 0, 0.2);
      }

      .password-section {
        margin-top: 3rem;
        padding-top: 2rem;
        border-top: 1px solid rgba(201, 202, 206, 0.2);
      }

      .password-form {
        display: flex;
        gap: 0.5rem;
        justify-content: center;
        flex-wrap: wrap;
        margin-top: 1rem;
      }

      .password-input {
        padding: 0.7rem 1rem;
        border: 1px solid rgba(201, 202, 206, 0.3);
        border-radius: 6px;
        background: rgba(0, 0, 0, 0.3);
        color: #FFFFFF;
        min-width: 200px;
      }

      .password-submit {
        background: rgba(119, 54, 229, 0.3);
        color: #FFFFFF;
        border: 1px solid rgba(119, 54, 229, 0.5);
        padding: 0.7rem 1.5rem;
        border-radius: 6px;
        cursor: pointer;
        font-weight: 500;
        transition: all 0.3s ease;
      }

      .password-submit:hover {
        background: rgba(119, 54, 229, 0.5);
        transform: translateY(-1px);
      }

      .contact-info {
        margin-top: 2rem;
        font-size: 0.9rem;
        color: #c9cace;
      }

      .contact-info a {
        color: #7736e5;
        text-decoration: none;
      }

      @media (max-width: 768px) {
        .container { padding: 1rem; }
        .lock-card { padding: 2rem 1.5rem; }
        .headline { font-size: 1.8rem; }
        .logo { font-size: 2.2rem; }
        .email-form, .password-form { flex-direction: column; }
        .social-grid { grid-template-columns: repeat(2, 1fr); }
        
        /* Mobile video optimization */
        .video-background video {
          display: none;
        }
        
        body {
          background: linear-gradient(135deg, #000000, #7736e5);
        }
      }
    </style>
  </head>
  <body>
    <div class="video-background">
      <video autoplay muted loop playsinline>
        <source src="https://cdn.shopify.com/s/files/1/0832/2543/6742/files/40328454209814" type="video/mp4">
      </video>
    </div>

    <main role="main" id="MainContent">
      {{ content_for_layout }}
      
      <div class="container">
        <div class="lock-card">
          <div class="logo">THAT DETAIL SHOP</div>
          
          <h1 class="headline">🔧 Upgrading Our Experience</h1>
          <p class="subtext">We're building something incredible. New features, better performance, and an enhanced shopping experience coming soon.</p>
          
          <a href="shop.app/m/thatdetailshop" target="_blank" class="cta-primary">
            🛒 Keep Shopping - Shop App
          </a>

          <div class="email-section">
            <h3>🚀 Get Launch Day Access</h3>
            <p style="color: #c9cace; margin-bottom: 1rem;">Be the first to experience our new site + get exclusive launch deals</p>
            
            {% form 'customer' %}
              <div class="email-form">
                <input type="email" name="contact[email]" placeholder="Enter your email" class="email-input" required>
                <button type="submit" class="email-submit">Notify Me</button>
              </div>
            {% endform %}
          </div>

          <div class="social-section">
            <h3>📱 Follow The Journey</h3>
            <div class="social-grid">
              <a href="https://www.youtube.com/c/thatdtailguy" target="_blank" class="social-link">YouTube</a>
              <a href="https://www.instagram.com/thatdtailguy/" target="_blank" class="social-link">Instagram</a>
              <a href="https://www.tiktok.com/@thatdtailguy" target="_blank" class="social-link">TikTok</a>
              <a href="https://www.facebook.com/thatdtailguy/" target="_blank" class="social-link">Facebook</a>
              <a href="https://www.linkedin.com/in/gary-passmore-619258247/" target="_blank" class="social-link">LinkedIn</a>
              <a href="https://x.com/thatdtailguy" target="_blank" class="social-link">X / Twitter</a>
              <a href="https://www.threads.net/@thatdtailguy" target="_blank" class="social-link">Threads</a>
              <a href="https://www.snapchat.com/add/thatdtailguy" target="_blank" class="social-link">Snapchat</a>
            </div>
          </div>

          <div class="affiliate-section">
            <h3>🛍️ Shop Our Curated Gear</h3>
            <div class="affiliate-links">
              <a href="https://www.amazon.com/shop/thatdtailguy" target="_blank" class="affiliate-link amazon">Amazon Storefront</a>
              <a href="https://walmrt.us/4gi75pG" target="_blank" class="affiliate-link walmart">Walmart Picks</a>
            </div>
          </div>

          <div class="password-section">
            <p style="color: #c9cace; margin-bottom: 1rem;">Team member or early access?</p>
            <form method="post" action="/password" class="password-form">
              <input type="password" name="password" placeholder="Enter password" class="password-input" required>
              <button type="submit" class="password-submit">Enter Site</button>
            </form>
          </div>

          <div class="contact-info">
            Need help? <a href="mailto:support@thatdetailshop.com">Contact our team</a>
          </div>
        </div>
      </div>
    </main>
  </body>
</html>